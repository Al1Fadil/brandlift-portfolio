import './globals.css';
export const metadata={title:'BrandLift — Creative with consequence.',description:'Cairo-based creative + performance agency working worldwide.'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
