import Image from 'next/image';import Link from 'next/link';import {notFound} from 'next/navigation';import {projects} from '@/data/projects';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export default async function Case({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();const idx=projects.indexOf(p);const next=projects[(idx+1)%projects.length];return <main className="case" style={{'--accent':p.accent} as React.CSSProperties}><nav><Link href="/" className="brand">BRANDLIFT</Link><div className="navlinks"><Link href="/work">ALL WORK</Link><Link href="/">HOME</Link></div></nav><header className="caseHero"><div className="caseCover"><Image src={p.cover} alt={p.title} fill priority sizes="100vw" quality={100} unoptimized/></div><div className="caseTitle"><span>{p.sector} / 2026 {p.concept?'· CONCEPT STUDY':''}</span><h1>{p.title}</h1><p>{p.services.join(' · ')}</p></div></header><section className="caseIntro"><div><span className="eyebrow">THE CHALLENGE</span><h2>Make the work feel clear before making it loud.</h2></div><div><span className="eyebrow">THE THINKING</span><p>{p.summary} Every execution was built around a specific job — positioning the offer, shaping perception, and creating a visual system that can survive beyond a single post.</p></div></section><section className="caseGallery">{p.gallery.map((img,i)=><div className={'galleryItem g'+i} key={img}><Image src={img} alt={`${p.sector} creative ${i+1}`} fill sizes="100vw" quality={100} unoptimized/></div>)}</section><section className="logic"><span className="eyebrow">THE CREATIVE LOGIC</span><h2>GOOD CREATIVE<br/><em>NEEDS A REASON.</em></h2><p>We build the angle first, then the execution. The visual language, hook, hierarchy and format should all point toward the same commercial idea.</p></section><Link href={'/work/'+next.slug} className="next"><span>NEXT PROJECT</span><h2>{next.title}</h2><b>ENTER ↗</b></Link></main>}
